{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": ["message"],
  "properties": {
    "message": {
      "type": "string",
      "minLength": 1,
      "description": "The user's message for this turn. `movate chat` auto-wraps free-form text into this field; prior turns reach the model via the chat REPL's history (not via this schema)."
    }
  }
}
