{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": ["reply"],
  "properties": {
    "reply": {
      "type": "string",
      "minLength": 1,
      "description": "The assistant's reply for this turn. ``movate chat`` renders this to the user and (when memory is on) appends it to conversation history as the assistant message."
    }
  }
}
