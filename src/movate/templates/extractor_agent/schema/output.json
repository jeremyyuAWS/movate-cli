{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": ["contact_name", "email", "intent", "urgency"],
  "properties": {
    "contact_name": {
      "type": ["string", "null"],
      "description": "Full name of the writer / contact, or null if not mentioned."
    },
    "email": {
      "type": ["string", "null"],
      "format": "email",
      "description": "Email address, or null. Strict format — the schema rejects non-email strings."
    },
    "intent": {
      "type": ["string", "null"],
      "enum": ["support_request", "feature_request", "billing", "general_inquiry", null],
      "description": "Classification of the writer's intent. Enum values are deliberate — keeps downstream consumers stable."
    },
    "urgency": {
      "type": ["string", "null"],
      "enum": ["low", "medium", "high", null]
    }
  }
}
