# Copy this file to `.env` in the same directory, paste the bearer
# token I sent you into MDK_TOKEN, save. Every other script in this
# directory auto-sources from here — you never touch the token again.

export MDK_BASE="https://movate-dev-api.victoriouswater-7958662f.eastus2.azurecontainerapps.io"
export MDK_TOKEN="mvt_live_PASTE_THE_BEARER_HERE"
